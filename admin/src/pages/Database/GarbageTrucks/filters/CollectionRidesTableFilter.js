import React from 'react';
import {Container} from "react-bootstrap";

const CollectionRidesTableFilter = () => {
  return (
    <Container>
      Collection rides table filter
    </Container>
  );
}

export default CollectionRidesTableFilter;
